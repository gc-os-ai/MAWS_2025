[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "maws"
version = "0.1.0"
description = "Modernised MAWS utilities"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]
include = ["MAWS*"]
